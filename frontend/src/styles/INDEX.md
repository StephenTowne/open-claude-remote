<!-- auto-doc: 文件增删时更新 -->
# frontend/src/styles/ - 全局样式与 CSS 变量

- variables.css: CSS 自定义属性，深色主题色板 + 状态色 + safe-area + 触摸目标尺寸 + 字体栈
- global.css: 全局 reset，box-sizing + overflow hidden + iOS zoom 防止 + pull-to-refresh 禁用 + 按钮/输入基础样式
