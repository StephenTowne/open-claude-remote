{
  "name": "claude-code-remote",
  "version": "0.1.0",
  "private": true,
  "description": "Remote control proxy for Claude Code CLI over LAN",
  "scripts": {
    "dev": "concurrently --kill-others --handle-input -n backend,frontend -c blue,green \"pnpm --filter backend dev\" \"pnpm --filter frontend dev\"",
    "build": "pnpm --filter shared build && pnpm --filter frontend build && pnpm --filter backend build",
    "start": "node backend/dist/index.js",
    "test": "pnpm --filter backend test",
    "stop": "bash scripts/stop.sh",
    "clean": "pnpm -r exec rm -rf dist"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "node-pty"]
  }
}
